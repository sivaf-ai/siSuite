server {
  listen 5173;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: ogni rotta sconosciuta torna index.html
  location / {
    try_files $uri $uri/ /index.html;
  }
}
