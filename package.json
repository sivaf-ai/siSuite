{
  "name": "sisuite",
  "version": "0.0.0",
  "private": true,
  "description": "siSuite — piattaforma gestione attività AI-first, mobile-first, multi-verticale",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "docker compose up",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
